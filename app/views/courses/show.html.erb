<p id="notice"><%= notice %></p>
<h1>Viewing Course: <%= @course.name %></h1>

<h3>Lecture Times</h3>
<ul>
  <% @course.lectures.each do |lecture| %>
    <li><%= link_to "#{lecture.day} at #{lecture.time}", course_lecture_messages_path(@course.id, lecture.id) %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_course_path(@course) %> |
<%= link_to 'Back', courses_path %>
